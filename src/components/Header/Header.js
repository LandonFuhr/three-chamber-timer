import { Layout, Typography } from "antd";

export const Header = () => {
  return (
    <Layout.Header>
      <Typography.Title style={{ color: "white" }}>
        Three-Chamber Timer
      </Typography.Title>
    </Layout.Header>
  );
};
